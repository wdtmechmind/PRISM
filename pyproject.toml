[project]
name = "prism"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
prism-collect = "prism.cli.collect:main"

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"